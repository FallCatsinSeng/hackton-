# Rust / Cargo build artifacts
target/
**/*.rs.bk

# Anchor build artifacts
.anchor/
target/
idl/

# Solana test ledger
test-ledger/

# Node.js
node_modules/
.yarn/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript compiled output
dist/
*.js.map

# Anchor keys (NEVER commit private keys!)
# But DO commit Anchor.toml program IDs
# wallet keypair (default location in container)
# target/deploy/*-keypair.json  ← uncomment if you don't want to commit keypair

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Package lock (optional — keep Cargo.lock for reproducibility)
# Cargo.lock  ← keep this committed for binary crates
